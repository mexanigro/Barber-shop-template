User-agent: *
Allow: /
Disallow: /admin
Disallow: /api/

# The per-tenant "Sitemap: https://{domain}/sitemap.xml" line is appended at
# build time by scripts/vite-plugin-seo.ts (closeBundle) using VITE_APP_URL /
# VERCEL_PROJECT_PRODUCTION_URL. The sitemap itself is served by api/index.ts.
