node_modules/
build/
dist/
functions/lib/
coverage/
.DS_Store
*.log
.env*
!.env.example
functions/.env
.cursor/mcp.json
a11y-report.json

# Firebase config with credentials — use VITE_FIREBASE_* env vars instead
firebase-applet-config.json

# Credenciales y dumps de config por cliente — JAMAS commitear
serviceAccountKey.json
*serviceAccount*.json
*.pem
*.key
live-hub-*.json
config-dump-*.json
matrix-config-*.json
live-config-*.json

# compiled server (produced by vercel-build / tsconfig.server.json)
server.js
.vercel

# Debug/audit screenshots
/*.png

# Tooling artifacts
ruvector.db
.playwright-mcp/
WEB-TEST-REPORT.md
tests/*
!tests/api-parity.test.ts
!tests/*.test.ts
!tests/orden/

# Capturas de QA visual por cliente: 1023 imagenes sin rastrear, no son producto (N05)
qa-screenshots/
qa-*/
# graphify code-graph output (no se commitea)
graphify-out/
# capturas QA en la raiz (limpieza-01, 2026-09-17)
/*.jpeg
dev-fixtures/media/
capturas-material/
