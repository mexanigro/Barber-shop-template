feat: add splash screen, sticky LandingBackdrop, and Navbar cleanup

- SplashScreen: solid dark bg, clip-path logo reveal (inset draw), Caveat
  letter-by-letter name animation with auto-adjusted stagger, curtain exit
  (translateY -100%), no click/key dismiss, session tracked via splash-session.ts
- LandingBackdrop: sticky hero image shared between Hero + Services sections;
  ends naturally when the block ends (no full-page fixed position)
- Hero: omitBackground prop to skip duplicate image when inside LandingBackdrop
- Services: overFixedBackdrop prop for semi-transparent + backdrop-blur treatment
- App: splash shown once per hard load on landing route; LandingBackdrop activated
  when both showHero + showServices are enabled; fallback renders standalone
- Navbar: ThemeToggle removed from desktop toolbar, kept in mobile menu only
- types.ts / site.ts: SplashConfig type + splash block in BASE_CONFIG
- index.html: Caveat font added via Google Fonts
